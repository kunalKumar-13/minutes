# Copy to frontend/.env.local. Points the browser at the FastAPI backend.
NEXT_PUBLIC_API_URL=http://localhost:8000

# This deployment's own origin. Only needed when hosted: it backs `metadataBase`
# so the Open Graph and Twitter cards resolve absolutely. Unset is fine locally.
# NEXT_PUBLIC_SITE_URL=https://your-deployment.vercel.app
